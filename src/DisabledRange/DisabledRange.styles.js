import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    position: 'absolute',
    borderRadius: 0,
    flex: 1,
    overflow: 'hidden',
  },
  touchableContainer: {
    flex: 1,
    alignSelf: 'stretch',
  },
});

export default styles;
